#include "NEHttpData.h"
using namespace neapu;

HttpData neapu::HttpData::FromRaw(const ByteArray& _data)
{
    return HttpData();
}

ByteArray neapu::HttpData::ToRaw() const
{
    return ByteArray();
}
